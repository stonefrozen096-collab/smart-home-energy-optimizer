# Copy to .env and load with `export $(cat .env | xargs)`, or set these
# as environment variables in Railway's project settings.
BROKER_HOST=your-cluster-id.s1.eu.hivemq.cloud
BROKER_PORT=8883
BROKER_USERNAME=your-hivemq-username
BROKER_PASSWORD=your-hivemq-password
BROKER_USE_TLS=true
